---
apiVersion: "v2"
name: "components"
description: "chart to deploy components"
type: "application"

# This field is ignored, version and AppVersion are determined by the package version in components/package.json
version: "0.0.0"
