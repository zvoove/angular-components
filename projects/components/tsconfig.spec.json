{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "outDir": "../../out-tsc/spec",
    "types": ["vitest/globals", "node", "@angular/localize"],

    // To much errors for now, but we should enable this in the future.
    "strictNullChecks": false
  },
  "include": ["**/*.spec.ts", "**/*.d.ts"],
  "files": ["test-setup.ts"]
}
