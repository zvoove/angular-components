import { ZvTimeFormats } from './time-formats';

export const ZV_NATIVE_TIME_FORMATS: ZvTimeFormats = {
  parse: {
    timeInput: null,
  },
  display: {
    timeInput: { hour: '2-digit', minute: '2-digit' },
  },
};
